// Entry point of the device emulator. Device clients, the event generator and
// the fault-injection modes are wired here in TODO step 3; step 1 only
// establishes the package so the workspace, build and lint setup is exercised.
export const SERVICE_NAME = 'emulator';
