// Entry point of the socket ingest service. The socket server, message
// validation and the RabbitMQ publisher are wired here in TODO step 4; step 1
// only establishes the package so the workspace, build and lint setup is
// exercised.
export const SERVICE_NAME = 'ingest';
