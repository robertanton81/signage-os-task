{
  "name": "@telemetry/ingest",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "test": "vitest run --root ../.. apps/ingest",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@telemetry/shared": "workspace:*",
    "amqplib": "catalog:",
    "ws": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/ws": "catalog:",
    "vitest": "catalog:"
  }
}
